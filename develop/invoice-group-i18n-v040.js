(() => {
  'use strict';
  const BUILTIN_GROUPS={fees:{ca:'Honoraris',es:'Honorarios',en:'Fees',fr:'Honoraires',de:'Honorare',ru:'Гонорары',pt:'Honorários'},expenses:{ca:'Despeses',es:'Gastos',en:'Expenses',fr:'Frais',de:'Auslagen',ru:'Расходы',pt:'Despesas'},disbursements:{ca:'Suplerts',es:'Suplidos',en:'Disbursements',fr:'Débours',de:'Fremdgelder',ru:'Возмещаемые расходы',pt:'Suprimentos'},discounts:{ca:'Descomptes',es:'Descuentos',en:'Discounts',fr:'Remises',de:'Rabatte',ru:'Скидки',pt:'Descontos'}};
  const LEGACY_NAMES={Honoraris:'fees',Honorarios:'fees',Fees:'fees',Honoraires:'fees',Honorare:'fees','Гонорары':'fees','Honorários':'fees',Despeses:'expenses',Gastos:'expenses',Expenses:'expenses',Frais:'expenses',Auslagen:'expenses','Расходы':'expenses',Despesas:'expenses',Suplerts:'disbursements',Suplidos:'disbursements',Disbursements:'disbursements',Débours:'disbursements',Fremdgelder:'disbursements','Возмещаемые расходы':'disbursements',Suprimentos:'disbursements',Descomptes:'discounts',Descuentos:'discounts',Discounts:'discounts',Remises:'discounts',Rabatte:'discounts','Скидки':'discounts',Descontos:'discounts'};
  const normalizeLanguage=language=>['ca','es','en','fr','de','ru','pt'].includes(language)?language:'ca';
  function normalizedKey(group){if(!group)return null;if(BUILTIN_GROUPS[group.key])return group.key;if(LEGACY_NAMES[group.name])return LEGACY_NAMES[group.name];for(const [key,labels] of Object.entries(BUILTIN_GROUPS)){if(Object.values(labels).includes(group.labels?.ca)||Object.values(labels).some(v=>Object.values(group.labels||{}).includes(v)))return key}return null}
  function groupLabel(group,language){const lang=normalizeLanguage(language),key=normalizedKey(group);if(key)return BUILTIN_GROUPS[key][lang];return group?.labels?.[lang]||group?.labels?.ca||group?.name||group?.key||''}
  function defaultGroups(){return ['fees','expenses','disbursements','discounts'].map((key,order)=>({id:`group-${key}`,key,kind:'builtin',order}))}
  function migrateGroups(groups,lines,language){const source=Array.isArray(groups)&&groups.length?groups:defaultGroups();const migrated=source.map((group,order)=>{const key=normalizedKey(group);if(key)return{id:group.id||`group-${key}`,key,kind:'builtin',order:Number.isFinite(group.order)?group.order:order};return{id:group.id||`group-custom-${order}`,key:group.key||`custom-${order}`,kind:'custom',order:Number.isFinite(group.order)?group.order:order,labels:group.labels||{[normalizeLanguage(language)]:group.name||`Group ${order+1}`}}});const nameToKey=new Map();for(const group of migrated){const key=normalizedKey(group);if(key)for(const label of Object.values(BUILTIN_GROUPS[key]))nameToKey.set(label,key);else for(const label of Object.values(group.labels||{}))nameToKey.set(label,group.key)}const migratedLines=(lines||[]).map(line=>({...line,groupKey:BUILTIN_GROUPS[line.groupKey]?line.groupKey:(nameToKey.get(line.group)||LEGACY_NAMES[line.group]||'fees')}));return{groups:migrated,lines:migratedLines}}
  function setCustomGroupLabel(group,language,value){if(!group||normalizedKey(group))return group;group.labels=group.labels||{};group.labels[normalizeLanguage(language)]=String(value||'').trim();return group}
  function translateBuiltInGroupName(name,language){const key=LEGACY_NAMES[name]||name;return BUILTIN_GROUPS[key]?.[normalizeLanguage(language)]||name}
  window.PYCInvoiceGroupI18N={BUILTIN_GROUPS,LEGACY_NAMES,defaultGroups,groupLabel,migrateGroups,setCustomGroupLabel,translateBuiltInGroupName};
})();