name: Add global search

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/add-global-search.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Add search UI and logic
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('index.html')
          text